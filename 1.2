#include <iostream>
#include <fstream>
#include <string>
#include <set>
#include <cctype>
using namespace std;

bool isConstant(string s) {
    int i = 0;
    if (s[0] == '+' || s[0] == '-') i++;
    for (; i < s.length(); i++)
        if (!isdigit(s[i])) return false;
    return true;
}

int main() {
    ifstream fin("input.txt");
    string word;
    set<string> constants;

    while (fin >> word) {
        if (isConstant(word))
            constants.insert(word);
    }

    cout << "Constants:\n";
    for (auto c : constants)
        cout << c << " ";

    fin.close();
    return 0;
}
